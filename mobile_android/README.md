# Open this folder in Android Studio to build CareerPilot Remote (APK).
#
# Quick path from repo root:
#   build_remote_apk.bat
#
# Or: Android Studio → Open mobile_android → Build → Build APK(s)
#
# The WebView loads bundled assets from app/src/main/assets/www
# (synced from ../remote_ui). User enters host URL + REMOTE_API_TOKEN in the UI.
